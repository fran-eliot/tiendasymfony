<form method="post" action="{{ path('app_fabricante_delete', {'id': fabricante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fabricante.id) }}">
    <button class="btn">Delete</button>
</form>
